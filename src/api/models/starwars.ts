export interface Character {
  name: string;
  homeworld: string;
}

export interface Planet {
  name: string;
  climate: string;
  terrain: string;
  latitude: number;
  longitude: number;
}